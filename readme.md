# Emoji Encyclopedia

## Setup

```bash
npm install
npm run dev
```

If you encounter registry access errors (e.g., 403 Forbidden), ensure your npm proxy settings are cleared:

```bash
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY npm_config_http_proxy npm_config_https_proxy
```

Then retry installation.
